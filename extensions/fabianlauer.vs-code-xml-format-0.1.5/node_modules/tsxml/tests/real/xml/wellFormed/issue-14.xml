<!-- see https://github.com/FabianLauer/tsxml/issues/14 -->
<StackLayout>
	<Label text="<" />
	<Label text="label using < "></Label>
    <Label text="label using < " />
    <Label text="label using multiple < < " />
    <Label text="<embedded xml='in attribute'>" />
    <Label visibility="p.cond < 1 ? 'visible' : 'collapsed'" />
    <Label visibility="{{ p.cond < 1 ? 'visible' : 'collapsed' }}"  />
    <Label text="label using < " visibility="{{ p.cond < 1 ? 'visible' : 'collapsed' }}"  />
    <Label text="label using != " visibility="{{ p.cond != 1 ? 'visible' : 'collapsed' }}" />
</StackLayout>