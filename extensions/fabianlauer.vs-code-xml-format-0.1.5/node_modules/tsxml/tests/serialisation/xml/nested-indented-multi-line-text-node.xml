<example>
Test text content
	that spans across more
than just a single
	line.
</example>
@@ COMPACT @@
<example>Test text content that spans across more than just a single line.</example>
@@ DEFAULT @@
<example>
	Test text content
	that spans across more
	than just a single
	line.
</example>